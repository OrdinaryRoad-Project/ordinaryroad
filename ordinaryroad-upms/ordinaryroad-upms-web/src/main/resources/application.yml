# Tomcat
server:
  port: 9401
# Spring
spring:
  application:
    # 应用名称
    name: ordinaryroad-upms
  profiles:
    # 环境配置
    active: dev
  #  cloud:
  #    nacos:
  #      discovery:
  #        instance-id: ${spring.application.name}-${spring.cloud.client.ip-address}-${server.port}
  #        prefer-ip-address: true
  #        hostname: 127.0.0.1
  #        port: 8500
  #        heartbeat:
  #          enabled: true
  #        service-name: ${spring.application.name}
  # 数据库配置
  datasource:
    dynamic:
      datasource:
        master:
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://localhost:3306/ordinaryroad_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
          username: root
          password: root